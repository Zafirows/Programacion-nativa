fun main(parametro: Array<String>) {
    // Un ciclo for que recorre los números del 1 al 100 (incluidos ambos extremos)
    for(i in 1..100)
        println(i)  // Imprime cada número en la consola
}
