//CAPITULO 15 -Extensiones Kotlin para Android (Completo)
// Importa extensiones sintéticas para acceder directamente a las vistas del layout activity_main
/*import kotlinx.android.synthetic.main.activity_main.*

class MainActivity : AppCompatActivity() {

    // Método que se llama al crear la actividad
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

        // Establece el layout activity_main como contenido de esta actividad
        setContentView(R.layout.activity_main)
        
        // Accede directamente al TextView con id 'textView' y le asigna un texto
        textView.text = "Hola Kotlin"

        // Configura el botón con id 'button' para que al hacer click muestre un mensaje Toast
        button.setOnClickListener { 
            Toast.makeText(this, "Click!", Toast.LENGTH_SHORT).show()
        }
    }
}
*/